# CONTENT => Flipkart Arch.


### STEP 1

- user opens `https://www.flipkart.com` => The browser performs a DNS lookup to resolve the domain name. The DNS provider (like Route53, Cloudflare, or Akamai) returns the IP of
the nearest CDN (Content Delivery Network) node — so the user connects to the closest server geographically for fast loading.
